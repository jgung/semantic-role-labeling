{
  "max_epochs": 500,
  "batch_size": 80,
  "keep_prob": 0.9,
  "max_length": 100,
  "num_buckets": 100,
  "lstm_num_layers": 8,
  "lstm_hidden_dim": 300,
  "features": [
    {
      "name": "words",
      "dim": -1,
      "vocab_size": -1,
      "initializer": "vectors.pkl"
    },
    {
      "name": "markers",
      "dim": 100,
      "vocab_size": 2
    }
  ]
}